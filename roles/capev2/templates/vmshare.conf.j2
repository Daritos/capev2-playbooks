server {
   listen {{ cape_vm_subnet_ip }}:8080;

   root /opt/vmshare;

   location / {
         try_files $uri $uri/ =404;
         autoindex on;
         autoindex_exact_size off;
         autoindex_localtime on;
   }
}
